const languages = [{ code: 'en', label: 'English' }, { code: 'hi', label: 'Hindi' }, { code: 'es', label: 'Spanish' }, { code: 'fr', label: 'French' }, { code: 'de', label: 'German' }];
function Slider({ label, value, min, max, step, onChange, suffix = '' }) { return <label className="block"><div className="mb-2 flex justify-between text-xs font-semibold uppercase tracking-wider text-slate-500"><span>{label}</span><span className="text-neon">{value}{suffix}</span></div><input type="range" min={min} max={max} step={step} value={value} onChange={(e) => onChange(Number(e.target.value))} className="w-full accent-neon"/></label>; }
export default function Selectors({ settings, setSettings }) { const update = (key, value) => setSettings((s) => ({ ...s, [key]: value })); return <div className="space-y-6"><label className="block"><span className="mb-2 block text-xs font-semibold uppercase tracking-wider text-slate-500">Language</span><select value={settings.language} onChange={(e) => update('language', e.target.value)} className="w-full rounded-xl border border-white/10 bg-white/[0.05] px-4 py-3 text-white outline-none focus:border-neon/60">{languages.map((l) => <option className="bg-panel" value={l.code} key={l.code}>{l.label}</option>)}</select></label><label className="block"><span className="mb-2 block text-xs font-semibold uppercase tracking-wider text-slate-500">Voice</span><select value={settings.voice} onChange={(e) => update('voice', e.target.value)} className="w-full rounded-xl border border-white/10 bg-white/[0.05] px-4 py-3 text-white outline-none focus:border-neon/60"><option className="bg-panel">Standard</option></select></label><div className="space-y-5 border-t border-white/10 pt-5"><Slider label="Speaking speed" value={settings.speed} min="0.5" max="2" step="0.1" suffix="×" onChange={(v) => update('speed', v)} /><Slider label="Pitch" value={settings.pitch} min="0.5" max="2" step="0.1" suffix="×" onChange={(v) => update('pitch', v)} /><Slider label="Volume" value={settings.volume} min="0" max="1" step="0.1" suffix="" onChange={(v) => update('volume', v)} /></div></div>; }
