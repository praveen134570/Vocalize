import { Download, Headphones } from 'lucide-react';
import { API_URL } from '../services/api';
export default function AudioPlayer({ audio }) { if (!audio) return <div className="flex min-h-[128px] items-center justify-center rounded-2xl border border-dashed border-white/10 text-sm text-slate-600"><Headphones size={18} className="mr-2"/> Your generated audio will appear here</div>; const url = `${API_URL}${audio.audio_url}`; return <div className="rounded-2xl border border-neon/20 bg-neon/[0.06] p-5"><div className="mb-3 flex items-center justify-between"><span className="text-sm font-semibold text-white">Ready to listen</span><a href={url} download={`vocalize-${audio.id}.mp3`} className="flex items-center gap-2 text-xs font-bold uppercase tracking-wider text-neon hover:text-white"><Download size={15}/> Download</a></div><audio controls src={url} className="h-10 w-full" /></div>; }
