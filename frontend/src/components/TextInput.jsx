export default function TextInput({ text, setText }) { const words = text.trim() ? text.trim().split(/\s+/).length : 0; return <div className="relative"><textarea value={text} onChange={(e) => setText(e.target.value.slice(0, 5000))} placeholder="Type or paste the words you want to hear..." className="min-h-[280px] w-full resize-y rounded-2xl border border-white/10 bg-white/[0.035] p-5 pb-14 text-[17px] leading-8 text-white outline-none transition placeholder:text-slate-600 focus:border-neon/60 focus:ring-2 focus:ring-neon/10" aria-label="Text to synthesize"/><div className="pointer-events-none absolute bottom-4 left-5 right-5 flex justify-between text-xs font-medium text-slate-500"><span>{words} {words === 1 ? 'word' : 'words'}</span><span className={text.length >= 4800 ? 'text-amber-300' : ''}>{text.length.toLocaleString()} / 5,000</span></div></div>; }
